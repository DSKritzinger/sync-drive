FROM node:22.23.2-alpine3.23

LABEL org.opencontainers.image.title="Sync Drive auth server" \
	  org.opencontainers.image.description="Self-hosted Google OAuth broker for the Sync Drive Obsidian plugin" \
	  org.opencontainers.image.source="https://github.com/DSKritzinger/sync-drive"

ENV NODE_ENV=production

WORKDIR /app

COPY package.json server.js ./

USER node

EXPOSE 8787

HEALTHCHECK --interval=30s --timeout=5s --start-period=5s --retries=3 \
	CMD ["node", "-e", "const port = process.env.PORT || 8787; fetch(`http://127.0.0.1:${port}/api/ping`).then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"]

CMD ["node", "server.js"]
